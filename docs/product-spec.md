# 무모한 시식가 — 제품 정책 및 기획 문서

## 1. 서비스 개요

**무모한 시식가**는 친구, 연인, 가족끼리 비공개 방을 만들어 맛집 리스트를 함께 기록하고 공유하는 프라이빗 맛집 리뷰 플랫폼입니다.

- **도메인**: https://musikga.kr
- **인증**: 카카오 OAuth (카카오 로그인만 지원)
- **호스팅**: Cloudflare Pages (프론트) + Render (백엔드) + Cloudflare R2 (이미지)

---

## 2. 페이지 구조

### 공개 페이지 (비로그인 접근 가능, SEO 색인)

| 경로 | 설명 | SEO 우선순위 |
|------|------|:----------:|
| `/` | 홈 — 랜딩 + 로그인 시 대시보드 | 1.0 |
| `/about` | 서비스 소개 + 비교 섹션 | 1.0 |
| `/discover` | 맛집 추천 (공개 방 기반) | 0.8 |
| `/rankings` | 유저 랭킹 + 업적 | 0.8 |
| `/use/friends` | SEO 랜딩: 친구랑 맛집 공유 | 0.9 |
| `/use/couples` | SEO 랜딩: 커플 맛집 리스트 | 0.9 |
| `/use/team` | SEO 랜딩: 회사 점심 맛집 | 0.9 |
| `/cs` | 문의하기 + FAQ | 0.5 |
| `/privacy` | 개인정보처리방침 | 0.5 |
| `/rooms/public` | 공개 방 목록 | 색인 |
| `/rooms/public/[id]` | 공개 방 상세 (읽기 전용) | 동적 |

### 인증 필요 페이지 (noindex)

| 경로 | 설명 |
|------|------|
| `/rooms` | 내 방 목록 + 생성/입장 |
| `/room?id=xxx` | 방 상세 — 식당/리뷰/통계/멤버/투표/타임라인 |
| `/room/add?roomId=xxx` | 식당 등록 + 리뷰 위저드 (3단계) |
| `/room/restaurant?roomId=xxx&id=xxx` | 식당 상세 — 방문/리뷰/지도 |
| `/join?code=xxx` | 초대 코드 입장 |
| `/profile` | 프로필 수정/테마/계정 관리 |
| `/login` | 카카오 OAuth 콜백 처리 |
| `/admin` | 관리자 대시보드 (admin 역할만) |

---

## 3. 인증 정책

### 카카오 OAuth 흐름

```
카카오 로그인 → 인가 코드 → 카카오 토큰 → 유저 정보 → JWT 발급 → 프론트 리다이렉트
```

### 토큰 정책

| 토큰 | 만료 | 저장 |
|------|------|------|
| Access Token | 15분 | localStorage |
| Refresh Token | 7일 | localStorage + DB (bcrypt 해시) |

### 토큰 갱신

- Access Token 만료 **1분 전**에 프론트에서 선제 갱신
- 401 응답 시 Refresh Token으로 재시도 → 실패 시 로그아웃
- 동시 401 발생 시 refresh 요청 **1회만** 실행 (mutex)

### 로그아웃

1. FCM 토큰 서버에서 삭제
2. localStorage 토큰 삭제
3. 서버 DB에서 refresh token 무효화 (fire-and-forget)

---

## 4. 방(Room) 권한 체계

### 역할별 권한

| 기능 | owner | manager | member |
|------|:-----:|:-------:|:------:|
| **방 관리** | | | |
| 방 이름 변경 | O | X | X |
| 방 삭제 | O | X | X |
| 초대 코드 재생성 | O | X | X |
| 방장 위임 | O | X | X |
| 멤버 역할 변경 | O | X | X |
| 멤버 강퇴 | O | X | X |
| 공개 방 토글 | O | X | X |
| 방 나가기 | X (삭제/위임 필요) | O | O |
| **식당** | | | |
| 등록 | O | O | O |
| 수정 | O (전체) | O (전체) | 본인만 |
| 삭제 | O (전체) | O (전체) | 본인만 |
| **방문 기록** | | | |
| 등록 | O | O | O |
| 수정 | O (전체) | O (전체) | 본인만 |
| 삭제 | O (전체) | O (전체) | 본인만 |
| **리뷰** | | | |
| 작성 (방문당 1인 1개) | O | O | O |
| 수정 | 본인만 | 본인만 | 본인만 |
| 삭제 | O (전체) | O (전체) | 본인만 |
| **투표** | | | |
| 생성 | O | O | O |
| 투표 | O | O | O |
| 마감 | 생성자만 | 생성자만 | 생성자만 |

**기획 의도**: 리뷰 수정만 본인 전용 — 리뷰는 개인 의견이므로 타인이 수정하면 안 됨. 삭제는 관리 목적으로 매니저 이상 허용.

### 방 제한

| 항목 | 값 | 기획 의도 |
|------|---|----------|
| 최대 인원 | 2~20명 (방별 설정, 기본 4명) | 소규모 친밀 그룹 기본값, 필요 시 확장 |
| 유저당 최대 방 수 | 30개 | 어뷰징 방지 |
| 강퇴 유저 재입장 | 차단 (RoomKick) | 분쟁 방지 |

### 초대 코드

- **형식**: 8자 hex (`crypto.randomBytes(4)`)
- **만료**: 무기한
- **재생성**: 방장만 가능 (기존 코드 즉시 무효화)

---

## 5. 이미지 업로드 정책

### 아키텍처 (Presigned URL)

```
프론트: 파일 선택 → 브라우저에서 압축 → 서버에 URL 요청
서버: presigned PUT URL 발급 (5분 유효)
프론트: R2에 직접 업로드 (서버 부하 0) → imageUrl을 API에 전달
```

### 압축 정책 (프론트 — browser-image-compression)

| 항목 | 값 |
|------|---|
| 최대 파일 크기 | 500KB |
| 최대 해상도 | 1200px |
| 포맷 | WebP 변환 |
| 처리 | Web Worker (메인 스레드 안 막음) |

### 폴더 구조 (R2)

```
fearless-tasting-images/
├── profiles/{userId}.webp              # 프로필 (덮어쓰기)
├── reviews/{userId}/{ts}-{hash}.webp   # 리뷰 사진 (최대 3장)
└── restaurants/{restaurantId}.webp     # 식당 대표사진 (덮어쓰기)
```

### 허용 형식

- `image/jpeg`, `image/png`, `image/webp`만 허용
- 그 외 형식은 400 에러

### 정리 정책

| 시점 | 처리 |
|------|------|
| 리뷰 삭제 | 해당 리뷰 이미지 R2에서 삭제 |
| 회원 탈퇴 | 프로필 + 전체 리뷰 이미지 삭제 |
| 프로필 변경 | 같은 키(`profiles/{userId}.webp`)로 덮어쓰기 |

### Rate Limit

- presigned URL 발급: **20회/분**

---

## 6. 리뷰 시스템

### 리뷰 입력 항목

| 항목 | 필수 | 범위/제한 |
|------|:----:|----------|
| 종합 평점 | O | 1~5 (정수) |
| 한줄 리뷰 | X | 최대 2000자 |
| 재방문 의사 | X | 또 가고 싶어요 / 글쎄요 |
| 맛 | X | 1~5 |
| 가성비 | X | 1~5 |
| 서비스 | X | 1~5 |
| 청결 | X | 1~5 |
| 접근성 | X | 1~5 |
| 또 먹고 싶은 메뉴 | X | 최대 200자 |
| 다음에 시켜볼 메뉴 | X | 최대 200자 |
| 사진 | X | 최대 3장 (WebP 압축) |

**기획 의도**: 별점만으로도 리뷰 작성 가능 (진입 장벽 최소화). 세부 평점은 같은 별 재클릭 시 취소 가능.

### 제약

- 방문당 1인 1리뷰 (중복 작성 시 409)
- 리뷰 수정은 본인만 가능 (매니저/방장도 불가)

---

## 7. 알림 시스템

### 알림 발생 이벤트

| 이벤트 | 메시지 형태 | 수신자 |
|--------|-----------|--------|
| 멤버 입장 | "OO님이 방에 참여했습니다" | 방 멤버 전원 (본인 제외) |
| 식당 등록 | "OO님이 'XX' 식당을 등록했습니다" | 방 멤버 전원 (본인 제외) |
| 방문 추가 | "OO님이 'XX' 방문을 기록했습니다" | 방 멤버 전원 (본인 제외) |
| 리뷰 작성 | "OO님이 'XX'에 리뷰를 남겼습니다 (N점)" | 방 멤버 전원 (본인 제외) |

### FCM 푸시

- **발송**: DB 알림 저장 후 fire-and-forget으로 FCM 발송
- **수신 설정**: 유저별 `pushEnabled` 토글 (프로필에서 on/off)
- **다기기 지원**: 유저당 여러 FCM 토큰 등록 가능
- **만료 토큰**: 발송 실패 시 자동 정리 (UNREGISTERED, INVALID_ARGUMENT)

### 프론트 알림

- 헤더 벨 아이콘 → 드롭다운 (최근 20개)
- 안 읽은 알림 빨간 뱃지
- 포그라운드 수신 시 뱃지 자동 갱신
- 백그라운드 수신 시 네이티브 알림 → 클릭 시 해당 방으로 이동

---

## 8. Rate Limit 정책

### 전역

- **120 요청/분** (모든 엔드포인트 기본)

### 엔드포인트별 오버라이드

| 범위 | 제한 | 대상 |
|------|------|------|
| 인증 | 5회/분 | 카카오 콜백, 토큰 갱신 |
| 생성 | 10회/분 | 방 생성, 입장, 식당 등록, 방문 추가, 리뷰 작성, 투표 생성 |
| 업로드 | 20회/분 | presigned URL 발급 |
| 건강 체크 | 무제한 | /health (ThrottlerSkip) |

**기획 의도**: IP 기반 추적. 초과 시 HTTP 429 응답.

---

## 9. 공유 정책

### 초대 링크 (멤버 초대)

- **복사 시 문구**: `🍽️ "방이름" 맛집 리스트에 초대합니다!\n우리만의 맛집을 함께 기록해요\n\n{URL}`
- **URL**: `https://musikga.kr/join?code={8자hex}`
- **Notion식 공유 팝오버**: 링크 복사 + 코드 재생성 + 공개 방 토글 + 멤버 미리보기

### 공개 방 (비로그인 열람)

- 방장/매니저가 공개 전환 가능
- 비로그인 사용자도 식당/리뷰 열람 가능 (쓰기 불가)
- 멤버 닉네임/프로필 미노출 (개인정보 보호)
- 리뷰 작성자 정보 미노출

---

## 10. SEO 전략

### sitemap 설정

- `@astrojs/sitemap` 빌드 시 자동 생성
- 공개 방 동적 sitemap (`/sitemap-public-rooms.xml`)
- priority: 홈/about 1.0, /use/* 0.9, discover/rankings 0.8

### 구조화 데이터 (JSON-LD)

- WebSite + SoftwareApplication (BaseLayout)
- FAQPage + BreadcrumbList (홈)
- AboutPage (소개)
- CollectionPage (랭킹, 맛집추천)
- ContactPage (문의)
- ItemList (공유 페이지)

### OG 메타

- 전역 OG 이미지: `/og-image.png` (1200x630, 다크 배경 + 앱 미리보기)
- 페이지별 title 50-60자, description 110-160자 최적화
- `/use/*` 페이지별 ogImageAlt 키워드 설정

### 내부 링크 전략

- 홈 추천 섹션 → `/use/*` 키워드 앵커 링크
- about 사용 사례 섹션 → `/use/*` 키워드 앵커 링크
- 홈 SEO 텍스트 → `/use/*` 키워드 앵커 링크

---

## 11. PWA 설정

| 항목 | 값 |
|------|---|
| display | standalone |
| orientation | portrait |
| theme_color | #111111 |
| background_color | #f7f8fa |
| scope | / |
| icons | 192x192, 512x512 (maskable 포함) |
| categories | food, lifestyle |
| viewport-fit | cover (노치 대응) |

### Service Worker

- 캐시 전략: stale-while-revalidate (정적), network-first (HTML)
- API 요청: 캐시 안 함
- 오프라인: `/` fallback
- FCM push/notificationclick 핸들러

---

## 12. 도메인 정책

| 항목 | 값 |
|------|---|
| 현재 도메인 | musikga.kr |
| 이전 도메인 | fearless-tasting.pages.dev |
| 리다이렉트 | 301 (Cloudflare Bulk Redirect) |
| SSL | Cloudflare 자동 |
| CDN | Cloudflare (자동) |

---

## 13. 통계/분석

### 방 통계 (`GET /rooms/:id/stats`)

- 요약: 식당/방문/리뷰 수, 평균 평점
- 멤버별 활동 분석
- 카테고리/지역 분포 (바 차트)
- 세부 평점 레이더 차트
- 요일별/월별 방문 패턴
- 대기시간 분포, 인기 메뉴
- TOP/BOTTOM 평점 식당, 재방문률 TOP
- 미리뷰 방문 알림
- 멤버별 행동 분석 (탐험가율, 카테고리 편향, 평점 성향, 리뷰 성실도, 요일 선호)

### 플랫폼 통계 (관리자 전용)

- DAU / WAU / MAU
- 전체 유저 수, 성장률

---

## 14. 관리자 기능

- **문의 관리**: 문의 목록 조회
- **공지 관리**: 공지 CRUD (제목, 내용, 활성화, 정렬)
- **통계**: DAU/WAU/MAU, 전체 유저 수
- **접근**: `role: 'admin'`인 유저만 (AdminGuard)
