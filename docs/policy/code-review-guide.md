# 코드 리뷰 가이드

PR 리뷰 시 아래 체크리스트를 기준으로 검토한다.

---

## P1: CRITICAL (즉시 수정 필요)

머지 차단. 반드시 수정 후 재리뷰.

| # | 항목 | 설명 |
|---|------|------|
| 1 | **null 참조 가능성** | DB 조회 결과 등 null 가능한 값을 체크 없이 사용 |
| 2 | **브레이킹 체인지** | Request/Response DTO 필드 제거 또는 타입 변경 |
| 3 | **N+1 쿼리** | 루프 내부 개별 DB 쿼리로 커넥션 풀 소진 위험 |
| 4 | **Race Condition** | 조회-업데이트 사이 트랜잭션 미사용 |
| 5 | **컴파일 에러** | duplicate identifier, 타입 에러 등 |
| 6 | **보안 취약점** | SQL Injection, XSS, 인증 우회 |
| 7 | **any/unknown 사용 금지** | `any`, `unknown` 타입 사용 절대 금지. 명확한 타입 정의 필수 |
| 8 | **NaN/nullable/falsy 검증** | NaN, nullable, undefined 참조 및 의도치 않은 falsy 값(`0`, `''`, `false`) 비교 검증 필수 |

## P2: HIGH (수정 권장)

Approve 가능하지만 수정 후 머지 권장.

| # | 항목 | 설명 |
|---|------|------|
| 9 | **인덱스 미적용** | WHERE, JOIN, ORDER BY 컬럼 인덱스 확인 |
| 10 | **중복 코드** | 동일/유사 로직 2회 이상 반복 |
| 11 | **함수 책임 과다** | 50줄 초과, 3개 이상 독립 작업 수행 |
| 12 | **DB 마이그레이션 누락** | 스키마 변경 시 migration 파일 없음 |
| 13 | **사이드 이펙트** | 의도하지 않은 동작 변경 |
| 14 | **에러 처리 누락** | try-catch 없이 외부 API 호출 |
| 15 | **DTO를 서비스에 직접 전달 금지** | Controller의 DTO를 Service로 바로 넘기지 말 것. 필요한 값만 개별 파라미터로 전달 |

## P3: LOW (개선 제안)

다음 PR에서 개선해도 무방.

| # | 항목 | 설명 |
|---|------|------|
| 16 | **변수명 불명확** | 축약어 과다, 의도 불분명 |
| 17 | **함수 주석 필수** | 모든 함수에 목적/파라미터/반환값 설명 주석 추가 |
| 18 | **API 문서 갱신** | API 변경 시 Swagger/OpenAPI 갱신 여부 확인 |
| 19 | **util 함수 관리** | 새로 만들기 전에 `@repo/utils` 기존 함수 재사용 가능 여부 먼저 파악 |
| 20 | **md 파일 갱신 확인** | 기능 변경 시 관련 `docs/*.md` 문서 갱신 및 추가 여부 확인 |
| 21 | **오타** | 철자 오류 |
| 22 | **불필요한 코드** | 사용하지 않는 import, 변수 |

---

## 리뷰 프로세스

### 리뷰어 규칙
- PR은 최소 1명 이상의 Approve가 있어야 머지 가능
- P1 항목이 1개라도 있으면 Request Changes
- P2 항목만 있으면 코멘트 후 Approve 가능
- P3 항목은 Nit으로 남기고 Approve

### PR 작성자 규칙
- PR 설명에 변경 사항, 테스트 방법을 명시한다
- Self-review를 먼저 수행한다
- P1 코멘트는 반드시 수정 후 재요청한다
- P2 코멘트는 수정하거나 사유를 남긴다
