name = "fearless-tasting-keep-alive"
main = "src/index.ts"
compatibility_date = "2024-12-01"

# 14분마다 실행 (Render 무료 플랜 15분 슬립 방지)
[triggers]
crons = ["*/14 * * * *"]

# PUBLIC_API_URL은 Cloudflare 대시보드 > Workers > Settings > Variables에서 설정
# 예: PUBLIC_API_URL = https://api.musikga.kr
