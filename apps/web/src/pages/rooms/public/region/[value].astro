---
export const prerender = false;

import PublicFacetHub from '@/components/PublicFacetHub.astro';

const { value } = Astro.params;
---

<PublicFacetHub facetType="region" value={decodeURIComponent(value ?? '')} />
