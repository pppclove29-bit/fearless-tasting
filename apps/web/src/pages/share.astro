---
export const prerender = false;
// /share?code=xxx → /share/xxx 리다이렉트 (기존 URL 호환)
const code = Astro.url.searchParams.get('code');
if (code) {
  return Astro.redirect(`/share/${code}`, 301);
}
---

<meta http-equiv="refresh" content="0;url=/" />
